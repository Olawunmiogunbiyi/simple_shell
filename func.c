#include "main.h"
/**
 * _strlen - the function to count the string
 * @s: the string to be countenate
 *
 * Return: Always void
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != '\0'; c++)
	{


	}
	return (c);
}

